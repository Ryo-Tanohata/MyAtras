# MyAtras — Claude Code 引き継ぎ

## 目的とユーザーの希望
日本科学未来館のジオ・コスモスのように、青い海と白い雲の地球をブラウザ（特にAndroid Chrome）で操作して、実際の雲の流れを見たい。静止画像を単に回す演出ではなく、SSECの異なる観測時刻を再生することが主目的。

GitHubへの公開コード登録はユーザーが了承済み。GitHub Pagesでの公開も了承済みで、`.github/workflows/pages.yml` が main への push ごと、`dist/` を公開する。観測の更新は自動では行わず、ワークフローを手動起動したときだけ `scripts/fetch-observations.cjs` で取得する（`fetch`・`frames`・`every` を指定。ランナーはSSECに到達できるが、エージェントのコンテナとスマホは到達できない）。取得に失敗したら同梱の観測をそのまま公開する。既定では取得物をcommitせず公開だけするので、実行が終われば同梱の観測に戻る。`commit` を真にすると取得した24枚を同梱として確定させる（約5MB）。いずれの場合も `records/published-observations.json` に公開した観測の時刻・SHA-256・取得元を記録する。毎日取得しないのは、1日分＝地球一周分で当初の目的を満たすため。公開先は https://ryo-tanohata.github.io/MyAtras/ で、ユーザーはこのURLをAndroid Chromeで開いて確認する。公式の未来館製品と表示しない。展示名「ジオ・コスモス」をサイト名や見出しに使わず、着想元として本文で触れるだけにする（画面上の名称は MYATRAS）。

## 起動
フレームワークやnpmインストールは不要。Python 3で `python -m http.server 8000 --directory dist` を実行し、ブラウザで http://localhost:8000 を開く。
単体HTMLは `python scripts/export-html.py` で生成。出力 `dist/myatras-clouds.html` はGit管理外。
`scripts/export-inline-earth.py` はChatGPT内表示専用で、Pillowと固定の `/workspace` 出力先を使用する。通常のブラウザ起動には不要。
ヘッドレスでの自動確認は `node tools/check-webgl.cjs`（`--shots` で画面保存、`--unity` で `dist/unity/` を確認、`--desktop` でPC幅）。npm依存はなく、Chromiumがあれば動く。

## 実装済み
- 生のWebGLで地球、ドラッグ・ピンチ、回転、照明を描画。
- 2026-09-22 22:00 〜 09-25 20:00 UTCの**71枚**（1時間ごと、70時間、13.9MB）の全球赤外線観測を順に再生。2026-09-25にワークフローの手動起動（`fetch`・`region` オン、**`wind_every 360`**、`commit` オン）で入れ替えた。SSECへの通信は**約320MB**で、前回の毎時取得1.66GBの約1/5。各時刻の風は**13/71時刻**・0.54MB（6時間ごと＋amv.jsonを作る中央時刻。風を持たない58時刻は、画像から測った動き→典型的な大気循環にフォールバックする）。観測間の動き70区間1.2MB、日本付近の高解像度12枚3.8MB（2.3km/画素）も同梱。以前の同梱は履歴にある。再生の速さは「1秒で何時間進むか」（1.5 / 4.5 / 9、既定4.5）で、観測の間隔が変わっても見え方が変わらない。両ページとも観測を並列で読み込む（JS版6本、Unity版8本同時）。日時は日本時間で表示。データは保存時点の観測であり常時最新ではない。
- 地表参考画像に観測輝度から白い層を合成。チェックボックス「元の白黒観測を表示」で元の観測に戻せる。地球回転は初期OFF。
- 最新取得、再生速度、時刻選択、キャッシュ、取得失敗時の保持。
- 「読み込む観測の数」は「最新を取得」で読み直すときの時刻数で、24（1日分・既定）か12（軽い）。SSECの一覧は直近24時間・1時間ごとなので24が上限。SSECが列挙した時刻しか使わず、足りない分を作り出さない。変更すると再生は止まる。
- 観測の切り替わりは380ms以下の溶け込み（`dist/observation-fade.js`）。画面上の2枚はどちらも実観測で、閾値は各観測に個別に適用し、描画結果だけを混ぜる。中間時刻の観測は作らない。日時は溶け込み先の観測を指す。溶け込むのは1時間後の観測へ進むときと、最新から最古へ戻るとき。後者は数日の飛びなので、2026-09-26（ユーザーの指摘「次が不自然に2枚目の様になる」）から**3秒かけて溶け込ませ、最初の観測を3秒保ち、「最初の観測へ戻っています · 最後の観測より約N日前の観測です」と出す**（`WeatherPlayback.RETURN_MS`、`ObservationFade.start` の第3引数。なめらかにのスイッチがオフでも行う）。時刻選択・再取得のときは即座に切り替える。「観測の切り替わりをなめらかに」で解除できる。Unity版も同じ規則（`unity/MyAtras/Assets/Scripts/ObservationPlayback.cs`）。
- 別モード「立体の模型」はSSEC AMV風向・風速を使う簡易移流。雲の形や量は模型で、実際の雲分布・気象予報ではない。

## 主要ファイル
- `dist/app.js`: 地球シェーダー・入力・表示統合。白い雲の合成と透かし保持もここ。
- `dist/storm-sim.js`: 観測の後の台風シミュレーション（段階②）。雲の移流と台風の雲。
- `dist/background-flow.js`: その雲を運ぶ背景の流れ（段階③、等価順圧渦度方程式）。`scripts/hindcast-flow.cjs` で採点。
- `scripts/fetch-jma-typhoon.cjs`: 同梱観測の最後の時刻に出ていた気象庁の台風予報を `dist/data/jma-typhoon.json` へ（観測が変わったときだけ公開ワークフローが取り、観測と一緒にcommit）。
- `dist/storms.js`: `storms.json` の読み出しと、台風の中心リング・軌跡の描画。地球と同じカメラで描くので印が観測と同じ場所に載る。
- `scripts/build-tendency.cjs`: IBTrACSから過去の台風の進路傾向を `dist/data/tendency.json` に。手動実行・commit。公開ワークフローは触らない。
- `unity/MyAtras/Assets/Scripts/StormMarks.cs`: Unity版の台風の印。JS版と同じ `storms.json` と `tendency.json` を読み、地球のブリット後に画面座標で重ねる（地球は全画面シェーダーで球体メッシュが無いため）。新しいシェーダーは `WebGlBuild.cs` の `ShaderNames` に登録しないとWebGLビルドで削除される。
- `dist/data-sources.js`: 同梱データの取得。配信時はJSONを読み、単体HTMLでは埋め込み済みの `window.GEO_*` をそのまま使う。
- `dist/weather.js`: 観測API、時刻、読み込み、エラー処理。
- `dist/weather-playback.js`: 時系列再生。
- `dist/weather/sequence/manifest.json`: 観測ファイル・時刻・取得元URL・SHA256。`dist/weather/snapshot.json` も同じ形式。
- `dist/cloud-model.js`, `dist/cloud-simulation.js`: 数値風による模型。`dist/data/amv.json` が同梱の観測風。
- `dist/index.html`, `dist/style.css`: UI。
- `scripts/export-html.py`: 単体HTML生成。埋め込み時にSHA256を再照合し、不一致ならビルドを止める。
- `scripts/build-amv.cjs`: 取得したAMV GeoJSONから `dist/data/amv.json` を作る。
- `scripts/find-storms.cjs`: 同梱観測から熱帯低気圧を見つけ `dist/data/storms.json` に書く。外部データを使わない。まだどの画面も読んでいない。
- `scripts/fetch-observations.cjs`: 同梱観測を最新に入れ替える。時系列・両製品のスナップショット・`--wind` で観測風。枚数は `--frames N`（既定13）、間隔は `--every M`（分）。SSECが出している間隔を実行時に表示するので、細かい観測があるかを見てから決められる。1枚約200KBで、ページは全枚数を読み込んでから再生を始める。画像の `RE-Time` ヘッダと要求時刻が一致しなければ停止し、別時刻として保存しない。古いフレームは削除。**SSECに到達できる機械で実行**（エージェントのコンテナからは403で不可）。実行後は `node scripts/build-motion.cjs`・`node scripts/find-storms.cjs` で派生データを作り直し、`python scripts/export-html.py` と `node tools/check-webgl.cjs` を通して `dist/` をcommit。**`THIRD_PARTY_NOTICES.md` の同梱一覧（枚数・期間・AMVの本数と時刻）も更新する** — 2026-09-21まで13枚・09-16のまま放置されていた。
- `tests/harness.cjs`: テスト用の最小DOMと通信スタブ。観測の中身は偽装しない。

## Unity版の方針
ユーザーの決定により、Unity版はWebGL（Unity 6の表示名は「Web」、実体は `BuildTarget.WebGL`）で作り、ビルド成果物を `dist/unity/` にcommitして同じPagesサイトの `/unity/` で公開する。JS版はルートのまま残す（併存）。
エージェントのコンテナにはUnity Editorもライセンスも無いためビルドはできない。ビルドはユーザーのローカルで行い、成果物をcommitする。詳細と必要なPlayer設定は `unity/README.md`。
2026-09-18、Windows（SHINYLABRY0）に Unity 6000.4.8f1 と Web Build Support を Unity Hub のCLIで導入し、初ビルドに成功（エラー0・警告0、約4MB）。Hub をVS Code拡張のシェルから起動すると `ELECTRON_RUN_AS_NODE=1` のせいで Node として動き `Cannot find module '--headless'` で失敗するので、この変数を外して起動する。
ビルドコマンド：`Unity.exe -batchmode -nographics -quit -projectPath unity/MyAtras -executeMethod MyAtras.WebGlBuild.Build -logFile <log>`。確認は `node tools/check-webgl.cjs --unity`（`dist/` を配信して `/unity/` を開く＝Pagesと同じ配置）。
Unity版は独自のWebGLテンプレート（`unity/MyAtras/Assets/WebGLTemplates/MyAtras`）でJS版と同じページ・同じ `style.css` の中に描画する。文字はすべてHTML側で、Unity内にフォントは持たない。操作はSendMessage、状態は `MyAtrasBridge.jslib` 経由でページへ渡す。
Unity版だけの機能として、各観測時刻の実際の太陽の位置で昼夜を描き（2026-09-22、ユーザーの希望で**既定はオフ**。平瞎に照らした展示として開き、JS版の既定と揃った。スイッチを入れるまで `#sunPoint` は「昼と夜の表示はオフです。」。C#の `sunlight` の初期値とテンプレートの `checked` の両方を外してあるので、最初の数フレームもオフ。これに伴って、地球が明るくなった分だけ紫と判定される地表の画素が増えたので、`tools/check-webgl.cjs` の「観測のある時刻に見込みを重ならない」検査は、別時刻の観測とでなく**同じ観測のON/OFF**を比べるように直した）、（`SolarPosition.cs`、天文年鑑の簡易式、`SolarPositionCheck.cs` で春分・秋分・夏至を検証）、夜側にNASA Black Marble 2016の街明かりを表示する。街明かりは固定画像で現在の観測ではないとページに明記している。大気はその太陽光のレイリー散乱とミー散乱の一次散乱を計算して描く（Nishita の方法、厚さ6倍・濃さ1/6で真上の光学的厚さは実際どおり、多重散乱は省略）。青い縁・夕焼け・太陽が裏にあるときの橙と青の輪は計算から出ており、色は指定していない。背景の星はエール輝星星表の6等星まで（`scripts/build-stars.py` で `dist/assets/stars.png` を生成、入力のSHA-256を照合）を恒星時で回して正しい位置に置き、明るい8星が予測画素に写ることを確認済み。雲の立体感と影は赤外線の明るさから推定した相対的な高さを10倍に誇張した演出で、実際の高さではないとページに明記している。夜側では街明かりが光のにじみとともに灯り（薄い雲の下も雲を照らす）、陸と海の境目が分かるよう Natural Earth の海岸線（`scripts/build-land.py` → `dist/assets/land.png`）を補助線として描く。観測風（`dist/data/amv.json`、JS版と同じファイル）を `WindField.cs` で1°格子にし（観測点から2.5°以内のみ、`WindFieldCheck.cs` で検証）、流れの筋として描く。風は1時刻だけの観測で、再生中の各時刻の風ではないとページに明記している。スマホでのカクつき対策として、太陽方向の光学的厚さは起動時に作る参照表（`AirDepthTable.cs`、`AirDepthCheck.cs` で検証）を引き、ページへの状態報告は変化時だけJSONを作り、フレーム時間に応じて描画解像度を自動で下げる（下限50%、ページに表示）。
2026-09-19、ユーザーの希望で「それっぽく見える」模型の方向へ振った。Unity版の既定は「風で流す模型」で、観測と観測の間を、前の観測を前へ・次の観測を後ろへ動かして重ねる動き補償の補間でつなぐ（シェーダーの `Pair`、`ObservationPlayback.cs` の `Model`）。動かす速さは第一に、前後2つの観測画像から推定した雲の模様の動き（`scripts/build-motion.cjs` → `dist/data/motion/`、8°の区画を3°ごとにずらし探索、信頼度つき）。推定できない所は各観測時刻のAMV（`dist/data/wind/`、前後2時刻の平均）で、観測点の近くはその値、9°以内はガウス重みで広げ、それも届かない所だけ典型的な大気循環（貿易風・偏西風・極東風、東西成分のみ）。観測ごとに必ず実観測に戻るので雲の配置は観測から離れない。間の動きは推定で観測ではないとページに常に明記し、「実観測だけ」に切り替えられる。一時停止はその場で止まる。
ユーザーは「雲が少し動いてまた同じ繰り返し」を気にしていた。原因は2つ：24時間のループの短さと、風が実際の雲の動きと合わない所で毎回観測へ引き戻されること。前者は3日分にして解消。後者は測定した：3時間の各区間で前の観測を風で流して次の観測と比べると、ずれは動かさない場合に比べて 典型的な循環だけ−12.8%、1時刻の風−14.2%、各時刻の風−16.2%。10°の区画ごとに最良の平行移動を当てはめた上限でも−31.7%で、3時間の変化の約7割は雲の発生・消滅・変形なので、流すだけでは揺り戻しはゼロにならない。2026-09-19、画像から動きを推定する方式を入れ、ずれは−32.4%（各時刻の風の約2倍）になった。推定に使った観測そのもので評価した数字で、観測の間をつなぐ用途ではこれが正しい評価だが、先を当てる精度ではない。推定の速さは中央値12 m/s・上位1%で38 m/s、中緯度で西風・熱帯で弱い東風と大気の流れに整合。2026-09-19、観測を1時間ごとにした。1コマあたりの変化（動かさない場合のずれ）は0.189から0.102へほぼ半減し、推定した動きで運んだ後に残るずれ（揺り戻しとして見える部分）は1コマあたり0.128から0.077へ約4割減。1時間ごとでは各時刻の風で−14.3%、推定した動きで−25.2%（動かさない場合比）。2026-09-19、ループのつなぎ目を直した。模型は「新しい方の48時間（丸2日）」をくり返し、最後の12時間と最初の12時間を、どちらも時間を進めたまま重ねて入れ替える（`ObservationPlayback.cs`、シェーダーの2つ目の層 `PairB`）。丸2日の窓なので重ねる2時刻は同じ時刻帯で、太陽の向きは混ぜても境界がほとんど動かない（赤緯の差0.8°程度）。重ねている間はページに「ループのつなぎ目：2日違いの観測を重ねて戻しています」と出る。「実観測だけ」は従来どおり全72時刻を流して先頭へ切り替える。最も古い12時刻は模型のループに入らない。Unityの検査 `Assets/Editor/PlaybackCheck.cs`（`-executeMethod MyAtras.PlaybackCheck.Run`）が窓・つなぎ目・戻る瞬間の連続性を確かめる。昼夜と星を切って1枚ごとの変化を測ると、実観測だけの切り替えは最大49.2%の跳び、模型のつなぎ目は最大33.0%でふだんの変化（上位10%で28.1%、最大38.1%）の範囲。観測を取り直したら `node scripts/build-motion.cjs` で作り直す（公開ワークフローは自動、`tests/build-motion.test.cjs` が古いままを検出）。

## 台風の検出（2026-09-21）
ユーザーの希望は「台風のそれっぽいモデル」。転向（西北西→北→北東）を出すには、まず台風がどこに居るかが要る。既存の動きの場は使えない — 区画が8°（約900 km）で渦より一桁大きく、しかも厚い雲の傘は模様が無いので区画照合が働かず、台風中心の移流量はほぼゼロだった。**つまり現在のアプリは台風を動かしていない**。観測と観測の間、台風はその場に留まり、見えている移動は100%観測そのものの移動。
`scripts/find-storms.cjs` は観測だけから台風を見つける（外部データなし → `dist/data/storms.json`）。全球512×512では台風は約7画素で、1枚の形からは熱帯対流と区別できない（丸く冷たい塊はアフリカにもITCZにもある）。区別は時間が与える：何日も形を保って一貫して移動するのは台風だけで、陸上対流は同じ場所で湧いて消え、クラスターは数時間で終わる。だから2段構えにした。連結成分は「まだ追っていない系に気づく」ためだけに使い、一度掴んだ系は予測位置の近傍で冷たい核を直接追う（距離でガウス重み付けした輝度重心）。前線と融合しても中心が前線側へ引かれないのはこの2段目の働き。採否は持続・欠測の少なさ・直進度・速さ・速度のばらつき・極向き成分・赤道からの距離（コリオリ力が弱い緯度8°以内を除外）で決める。
精度は気象庁の実況経路と照合した（**照合のみ。取得も同梱もしない**）。成熟期（09-17〜09-20、台風25号ドゥージェン）は**中央値74 km**（約1画素）、68時刻すべてが200 km以内、誤検出ゼロ。同じ台風の発達期（09-13〜09-16、別途72枚を取得して検証）は**中央値596 km**。シアーで深い対流が循環中心から離れ、対流の方を追ってしまうため。
**この2つを見分ける手段はまだ無い。** 各点に核の円形度を記録しているが、成熟期の中央値2.54に対し発達期2.22で分布がほぼ重なり、信頼度としては使えない。74 kmのときも596 kmのときも同じ顔をしている。閾値はこの2つの窓だけで選んでおり、過剰適合は否定できない。他の台風・ハリケーンは未検証（2026-09-17〜20は世界的にハリケーンがゼロで、見逃しの確認ができていない）。
観測を取り直したら `node scripts/find-storms.cjs` で作り直す（公開ワークフローは自動、`tests/find-storms.test.cjs` が古いままを検出）。
2026-09-21、JS版が `storms.json` を読むようにした（`dist/storms.js`）。スイッチ「台風の中心を重ねる（観測画像からの推定）」で、画面に出ている観測時刻の推定中心に琥珀色のリングを、そこまでの軌跡を点線で重ねる。**観測時刻に検出があるときだけ描く** — 時刻の間を補間せず、最後の観測より先へも進めない。検出のない時刻（同梱72時刻のうち3時刻）には何も描かず、その旨を表示する。緯度経度と「気象庁の発表でも予報でもありません」を印の隣に出し、全文は「この表示について」に畳む。画面の約束どおり、スイッチはスイッチの並びの末尾に置き、段落を間に挟まない。Unity版は未対応。

2026-09-22の新データで**赤道帯の規則を直した**。採否を「平均緯度が8°以上」で判定していたため、南米北部で7.4°Nまで下がる系が平均でだけ通った（ITCZの対流であって台風ではない）。`tests/find-storms.test.cjs` は元々**すべての点**が8°の外であることを要求していたので（古いデータでは自明に満たしていて露呈しなかった）、検出器を `minAbsLat >= 8` に合わせた。結果は**3系**：日本の南から北東へ転向した台風（57時刻、26.7°N 139.1°E → 36.2°N 142.3°E、1,094 km、32.4 km/h）と、**東太平洋の2系**（10°N台、西進）。前者は以前の検証窓の続きで信頼できるが、**後者は未検証**（照合したのは北西太平洋の1例だけ）。

## 転向モデル（2026-09-21）
成熟度の判定は**断念した**。512×512（68 km/画素）では台風の眼が1画素も写らず（成熟期全68時刻で0.000）、試した5指標（円形度・リング充填率・均一さ・300 km充填・周囲との際立ち）はすべて失敗、分離したものは逆向きだった（周囲の雲量を測っていた）。1.4 km/画素なら差は出るが（核の明るさ 発達期190 / 成熟期221）、それは**真の中心で測った場合のみ**で、我々が測れる検出位置は失敗時に600 km離れた対流の上にある。決定的なのは、手元の2つの窓で**成熟度と緯度が完全に交絡**していること（発達期は10–19°N、成熟期は20–29°Nで重なりゼロ）。台風1つでは何を作っても検証できない。
代わりに、ユーザーの提案で**過去のデータ**を使った。NOAAのIBTrACS（全球ベストトラック、1980年以降、137 MB、同梱しない）から、2.5°格子ごとに「その海域を過去の台風がどちらへ何km/hで進んだか」の中央値とばらつきを取り出す（`scripts/build-tendency.cjs` → `dist/data/tendency.json`、147 KB）。4,759個・225,747区間。出てくるのは教科書どおりの循環だが、描いたものではなく測ったものである：亜熱帯高気圧の南で西北西、西の縁で北、偏西風に入って北東かつ加速（16→41 km/h）。
**ばらつきこそが要点**。熱帯では5個中4個が中央値±45°に収まるのに、日本の南の転向域ではかろうじて半数。転向するかどうかは位置だけでは決まらないという事実で、線だけ描けば嘘になる。band50/band80として記録し、扇として描く。
気象庁の実況位置から傾向だけで進めると、12時間後126 km・24時間後180 km・45時間後109 kmで予報に追随した（予報円は45/65/155 km）。69時間後は863 kmで崩れる（実際は偏西風で60 km/hまで加速するが平均はそこまで速くない）。
JS版・Unity版の両方に実装（`dist/storms.js` の `StormOutlook`、`unity/MyAtras/Assets/Scripts/StormMarks.cs`、スイッチ「この先の見込みを重ねる（過去の台風の平均）」）。**観測が尽きた最後の時刻でのみ**48時間先まで描き、観測のある時刻には重ねない。これはこのアプリで初めての「観測より先」の表示なので、予報ではないことを画面に常時明記している。
**観測と予想の描き分けは4つの手がかりを重ねる**（色だけでは足りない — 色覚特性で区別できない場合があり、そもそも色の差は「重要度の差」に読まれて「事実か推測か」を伝えない）。色（琥珀＝観測／青紫＝予想）、線（連続＝観測／6時間ごとに飛ばす破線＝予想）、形（塗りつぶし＝観測／中抜き＝予想）、幅（1本＝観測／半透明の扇で塗る＝予想）。扇があること自体が「範囲」を意味し、観測には幅がない。境目は琥珀のリングが担う。
`tendency.json` のセルは**平坦な数値配列**（Unityの `JsonUtility` は辞書を読めないため）。116 KBで、必要になったときだけ読む。
残る課題は変わらず成熟度の判定で、それには性質の違う台風が3〜5個要る。`storms.json` は観測を取り直すと**上書きされる**ので、蓄積したいなら記録に残す仕組みの追加が要る（未実装）。
このPC（SHINYLABRY0）からはSSECに到達でき、Unityのビルドも回せる。エージェントのクラウドのコンテナからはどちらもできない。
Unity版は観測を独自取得せず、`dist/weather/` と `dist/data/` の同じファイルを読む。SHA-256マニフェストを唯一の正とし、両版が同じ観測を表示する状態を保つ。

## 台風オブジェクト（2026-09-26・段階①）
ユーザーの希望：十分に大きい台風が見えたあと、過去のデータとモデルで**消滅まで自然に動かす**。精密でなく、明らかな間違いのない抽象的なオブジェクトでよい（予測であることは了承済み）。3段で作る：①台風オブジェクト（本節）②雲をその流れで運ぶ（渦巻きが自然に出る）③背景の流れを順圧渦度方程式にして、いつまでも循環させる。
- 実装：`dist/typhoon.js`（位置・強さ・直近の動きだけを持つ物体、1時間ずつ進める）。動きは「その場所を過去の台風がどう動いたか」の2.5°格子の中央値で、**自分の動きが東向きに変わるまでは全台風、変わったあとは転向済みの台風**の格子に従う（転向後は北東へ加速する）。持続性は「その場所の動きからのずれ」を毎時 exp(-1/τ) ずつ減らす。強さは海上では同じ強さの台風がそこで6時間にどう変わったかの中央値、陸上は Kaplan–DeMaria の減衰式。終わりは 34 kt 未満、**その経路で過去の台風の半数が終わった地点**（1時間あたりの終わりやすさを場所ごとに実測した値で生存率を減らす）、または赤道から4°以内。
- データ：`scripts/build-typhoon.cjs` が IBTrACS から `dist/data/typhoon.json`（約470KB）を作る。**IBTrACSはこのコンテナから取れない（403）**ので、`.github/workflows/typhoon-model.yml`（手動起動）がランナーで取得・採点・構築・commitする。年1回程度で十分。
- 採点：`scripts/hindcast-typhoon.cjs`。1980–2014年だけで作り、**2015年以降の全台風**について64kt以上の12時間ごとの3,684の時点から走らせ、実際の経路と比べる。基準は「直進（持続）」と「全台風の平均だけ（今の見込みと同じ考え方）」。調整する数はτだけで、2005–2014年で24–120時間の誤差の平均が最小になる値を選ぶ（採点に使う年では選ばない）。結果は `records/typhoon-hindcast.json`。
- **現在の成績（7回目・τ=6）**：位置の中央値誤差 24h 189km / 48h 417km / 72h 645km / 120h 1016km。平均だけは 235 / 458 / 677 / 1036 で**全時間で上回る**。直進は 142 / 370 / 646 / 1375 で、72時間までは直進の方が近く、96時間以降はこちらが近い。**転向は実際の72%**（西向きで始まった2,743例のうち実際1,227、モデル889）。寿命の誤差は中央値31時間・偏り+2時間（一定値84時間で当てると39時間）。赤道越え0。例：フィリピンの東 18N 135E（100kt）→ 沖縄の西を北上 → 28.7Nで転向 → 日本海を北東へ → 174時間で終わる。実際の予報（気象庁など）は周りの気圧配置を使うのでこれよりずっと良い。
- **位置と転向は引き換え**：持続性τを長くすると直進が得意な数日間の位置は良くなるが、転向すべき台風がまっすぐ進む（2005–2014年で τ=0:68% / 6:61% / 12:52% / 24:42% / 48:26% / 96:7% の転向）。τは「実際の7割以上転向するものの中で位置が最良、なければ最もよく転向するもの」で選び、**最低6時間**を必須とした（0だと観測から切り替わる瞬間に向きが折れる）。
- **7回の採点で見つけたこと**（同じ過ちを繰り返さないため）：
  1. IBTrACSは**3時間ごと**（6時間ごとの解析の間に補間点）。「2点前＝12時間前」と数えて全例が落ち、**空の採点をcommitした**。いまは時刻で遡り、例が0なら失敗させる。テストの模擬データも3時間ごとにした（旧コードで落ちることを確認）。
  2. 「まだ西へ進んでいる台風」の中央値は**どの緯度でも西向き**（35Nでも−5km/h）。転向した台風はその瞬間にその集合から抜けるので、集合は転向しなかった台風だけになる。**分岐の中央値は分岐を表せない**。これに従う物体は決して転向しなかった（0/1,227）。全台風の中央値は転向域で東向きに変わる（25N −2.5 → 27.5N +1.6 → 30N +4.4km/h）。
  3. 強さの中央値変化では**終わりが見えない**（6時間ごとには稀な出来事なので中央値に出ない）。温帯・消散の再分類だけ数えると139,000区間中415件しかなく、212例が暖かい海を15日さまよった。34kt未満への低下と記録の途切れも終わりとして数えて2,643件にし、偏りが+26時間から+1時間になった。
  4. τを位置の誤差だけで選ぶと長い持続性が勝ち、転向すべき台風がまっすぐ進む（48時間で選んで転向31%）。転向の割合も条件にした（上の段落）。
- 模擬世界のテスト（`tests/typhoon.test.cjs` 12件）は答えの分かっている動き・陸・終わり方を確かめる。**模擬世界は地球全体を海として作っているので、実際の海岸線を知るモデルとは食い違う**（模擬の台風がアジアに上陸して転向前に消える）。実データでは経路も陸も同じなので起きない。

## 台風のシミュレーション表示（2026-09-26・段階②・JS版のみ）
スイッチ「観測の後もシミュレーションで続ける」（2026-09-26から**既定オン**。最初は「観測の後、台風を消滅まで進める」で既定オフだったが、ユーザーが見ていたのは最後の観測から3日前の観測への跳びだった）。再生が最後の観測まで来たところで止まらずに、雲を背景の流れで進め、その観測で見つかっている台風（`storms.json` の `last` が真の系、最大4個）を段階①の物体で消滅まで進める。**台風が無くても続ける**。長さは120時間、台風が長生きすればその終わり＋18時間、最長240時間（`HORIZON_HOURS`・`MAX_HOURS`）。それより長いと雲が観測の細かさを失う（緯度平均への緩和は240時間の時定数）。終わるとシミュレーションの雲を3秒かけて最初の観測へ溶け込ませる（`simShown`、最初の観測は下に即座に置く＝`simUnder`）。残り12時間になると「まもなく最初の観測へ戻ります」と出す。
- 実装：`dist/storm-sim.js`（GPU、1024×512の緯度経度格子）。風は3つの和：緯度ごとの典型的な循環（Unity版の `ClimateWind` と同じ曲線）＋台風の近く（900km）では台風の移動＋台風の回転（中心90kmで最大、外は r^-0.6、少し内向き）。
- **観測の雲を毎フレーム運び直さない**。最初の版はそうして、再標本化のたびにぼけて数秒で霧になった。いまは各点について「その空気が最後の観測でどこにあったか」（ずれの地図、16bitを2チャンネルに詰める）だけを運び、雲は観測から1回だけ読む。何日進めても観測の細かさが残る。台風自身の雲（計算で湧かせるもの）だけは普通に運ぶ。
- （2026-09-26に廃止、「台風の雲は観測を運ぶ」の節）台風の雲：強さに応じて中心の厚い雲（約250km）と、典型的な傾き（約22°）の2本の渦巻きの帯を湧かせ、回転が周りの観測の雲ごと巻き込む。**最初は帯を描かず回転だけで出るはずとしたが、小さな白い点にしかならなかった**（湧かせた雲は一様で、巻き込む模様がない）。消滅後は湧かなくなって薄れる。全体は120時間の時定数で緯度ごとの平均雲量へ近づく。
- 強さは画像から測れないので**80ktと仮定**。初速は最後の観測から11時間以上前の検出点との差で、**45km/hを上限**にした（東太平洋の系で検出が別の雲へ飛び、54km/hで南西へ走ったため）。
- 画面：日時は「シミュレーション +N時間（M/D HH:MM の観測から）」、再生の行に「予報ではありません」、雲はわずかに紫がかる、台風は紫の中抜きの輪（観測の琥珀の輪と区別）と24時間ごとの点。「この先の見込み」の扇はシミュレーション中は描かない。**一時停止はその場で止まる**（`WeatherPlayback.hold()`）。時刻を動かす・画像を変える・再取得すると終わる。
- 再生とのつなぎ：`WeatherPlayback.onLastFrame(time, resume)` が真を返すと再生は最後の観測のまま待ち（`waiting`）、`resume()` で先頭から再開する。待っている間の迷い込んだ `step()` は何もしない（`tests/weather-playback.test.cjs` で固定）。
- **単体HTML版には出さない**（模型が約470KB）。判定に `GEO_WEATHER_SEQUENCE` の有無を使ったら、配信版でもキャッシュ済みで定義されていて、配信版で模型が読まれなかった（`region()` も同じ潜在バグだった）。いまは単体HTMLが明示的に `window.GEO_STANDALONE=true` を書く。
- テクスチャユニット：スマホは8個しかないことがあるので、地球が使わない5・6と、借りて戻す3・4だけを使う。表示用は7。
- 確認：`tools/check-webgl.cjs` に11項目（戻るときの溶け込み2項目を含む）。以下は当初の9項目（スイッチ、開始、日時の表記、予報でない旨、雲が動く、観測が待つ、一時停止で止まる、印、終わったら観測へ戻る。単体HTMLではスイッチが出ないこと）。同梱観測では4個（21.3N 127E が101時間で台湾の北の大陸沿岸で衰弱、東太平洋2個・大西洋1個は56–77時間）。
- 背景の風は段階③で緯度だけの東西の流れから、うねって循環する流れに替えた（次節）。Unity版は未対応。

## 背景の流れ（2026-09-26・段階③・JS版のみ）
段階②の雲は緯度だけで決まる東西の風に乗っていたので、偏西風帯の雲が何日も引き伸ばされて細い筋になった。いまは `dist/background-flow.js` の流れに乗る。
- 方程式：**等価順圧渦度方程式**。渦位 q = ∇²ψ − ψ/Ld² + f を流れに沿って保存し、そこからψを逆算して風を得る。気圧の谷や尾根が動き、Rossby波が流れに逆らって西へ進み、ジェットはジェットのまま残る。雨・前線・加熱・上下の層は無い。変形半径 Ld=1000km（事前に選んだ値で、採点でも最良）。
- 数値：128×64（2.8125°）の格子。経度はFFT、緯度は三重対角でψを解く。qは半ラグランジュ（3次元の単位ベクトルで出発点をたどるので極も特別扱いしない、Catmull-Rom補間）。**出発点をたどる風は前2ステップから外挿した「ステップの途中の風」**（今の風だけでは2時間刻みで速い波が8%遅れた）。帯状平均だけ240時間でもとへ緩和し、波には触れない。2時間ごとに進め、1ステップ約6ms（このコンテナ）。
- 初期値：最後の観測までの6時間分の、観測画像から測った雲の動き（`dist/data/motion/`）を信頼度で平均し、信頼度の低い所は典型的な循環で埋め、回転成分だけを残す。最後の観測より12時間以上前の動きしか無いとき（「最新を取得」で観測が新しくなったとき）は使わず典型的な循環から始め、画面にそう出す。
- 採点（`scripts/hindcast-flow.cjs` → `records/background-flow-hindcast.json`）：同梱70時間の中の5つの開始点（3時間おき・重なりあり）から進め、12/24/48時間後に実際に測った動きとのRMSベクトル差（km/h）。流れ 20.3/25.0/27.1、始めの動きをそのまま保つ 21.6/28.0/29.9、典型的な循環 34.8/35.3/33.5。**全時間で持続を上回る**が、測った動き自体が推定で開始点も少ない。Ld無しは時間の外挿を入れる前は48時間で42.2に崩れた（原因は時間刻みの誤差で、直した後は28.2）。
- 物理の検査（`tests/background-flow.test.cjs` 7件）：Rossby波の西進速度が理論 −2Ω/(n(n+1)+a²/Ld²) と3%以内（実測 6.85° 対 理論 6.86°/12h）、極を越える流れ（Y₁¹）が理論どおり回る、帯状流が2日で0.05km/h未満しか変わらない、乱れたジェットが30日で発散も消滅もしない、GPUへの16bit詰めが0.02km/h以内。**同梱観測に依存しない**（公開ワークフローが取得後にテストを回すため）。
- 同梱観測から30日回すと、全エネルギー86%・渦のエネルギー47%・45°Nの帯状平均風は39km/hのまま・最大79km/h。場面は長くても1週間程度なので渦は十分残る。
- 見た目の測定（ヘッドレス、雲の南北方向と東西方向の変化の比、30–60°帯、観測時0.8前後、大きいほど東西の筋）：緯度だけの風は48時間で1.4・96時間で2.2、この流れは1.0–1.2・1.3–1.4。**筋の育ち方は約6割減ったがゼロではない**。雲は運ばれるだけで湧いたり消えたりしない（台風の周り以外）ため、シアーで伸びた雲はそのまま残る。雲の発生・消滅（例：正の渦度移流の所で湧かせる）は未実装。
- 台風の進路は段階①の物体のまま（採点済みのため）。背景の流れで台風を流すことはしていない。
- `tools/check-webgl.cjs` に「背景の流れが測った動き6時間分から始まり、先へ進む」を追加。

## 気象庁の予報に沿う台風・終わりで止まる（2026-09-26・JS版のみ）
ユーザーの指摘：シミュレーションの台風26号（スリゲ）が北西の大陸へ進んだが、気象庁（ウェザーニュースの図）は沖縄付近をゆっくり北上して30日に北東へ転向、本州の南へ。段階①の物体は過去の台風の平均と直前の動きしか知らず、今週の気圧配置を使わないので、転向を逃す28%の側に入った。ユーザーの選択は「A：気象庁の予報進路に沿わせる」と「2：終わりで止まる」。
- 取得：`scripts/fetch-jma-typhoon.cjs` が気象防災情報XMLの長期フィード（`extra_l.xml`）から「台風解析・予報情報」を集め、擾乱ごと（Headの `EventID`、熱帯低気圧の段階から同じ）に最新の報を残して `dist/data/jma-typhoon.json` に書く。1日より前の報と予報の切れた報は捨てる（フィードには数日分が残り、終わった台風の報も残るため）。**コンテナからは気象庁に届かない（403）**ので、公開ワークフローが毎回と**3時間ごとの schedule** で取得する（SSECには触れない。観測の取得は手動起動のときだけのまま）。公開するだけでcommitしない（commitされたファイルは空）。失敗しても空で書いて公開は続ける。
- 実データで初めて分かった3つの誤り（テストは実際の報を切り詰めた `tests/fixtures/jma-typhoon-2626.xml`）：①`<Head>` に `xmlns` 属性があり `<Head>` の正規表現が当たらなかった、②最大風速の要素は `unit` が `type` より先で、属性の順を決め打ちした正規表現が当たらなかった、③番号の無い熱帯低気圧の報を1つずつ別の嵐と数えた（`EventID` で解決）。
- 動かし方：`Typhoon.followForecast`（`dist/typhoon.js`）。実況と各予報時刻の中心を時間についての3次エルミート曲線でつなぎ（各中心をちょうど通る）、最大風速を線形に。観測で見つけた位置が予報から離れていれば、その差を12時間かけて0にする（見つけた位置と気象庁の中心は約100km違い、実況の4時間後に合わせたら行き過ぎて戻った）。最後の予報時刻でまだ台風（`台風(TS/STS/TY)`）なら段階①の物体で続け、熱帯低気圧などならそこで終わる。予報が始まる時刻より24時間以上前、または終わる12時間前より後の観測からは使わない。見つけた台風が予報の中心から600km以内なら同じ台風とする。気象庁の予報が無い台風（東太平洋など）は従来どおり。紫の点は予報の各時刻（予報の先は1日ごと）。
- 表示：注記に「台風26号（スリゲ）は気象庁の予報（M/D HH:MM発表）の進路と強さに沿って動かしています（予報の先は過去の台風の動き方）。出典：気象庁「台風解析・予報情報」を加工して作成」。再生の行は「台風は気象庁の予報進路に沿って動き、雲は計算です · このアプリの予報ではありません」。
- **終わりで止まる**：シミュレーションは最後の時刻（通常+120時間、台風が長ければ最長240時間）でその場に止まり、再生ボタンが「↺ 観測の最初から再生」になる（`WeatherPlayback.finish()`／`restart()`／`onRestart`、`StormSimulation.finished`）。押すと3秒で最初の観測へ溶けて戻る。スイッチがオフのときのループの戻りは3秒の溶け込みのまま。
- **自動取得の不具合を直した**：「1時間ごと」の自動取得は最後の確認時刻が0から始まっていたので、開いてから初めてページが画面に戻ったとき（画面録画を始めるためにアプリを切り替えた等）すぐにSSECの最新24時刻へ入れ替わった。同梱の台風と雲の動きはその時刻を知らないので、台風が消え、背景の流れが緯度だけの東西の風になっていた（ユーザーの動画で24時刻の再生・台風の輪なし・強い東西の筋として確認）。同梱の観測で開いたときは開いた時刻から1時間を数える（`dist/weather.js`、`tests/weather.test.cjs`）。**スマホはSSECに届く可能性が高い**（本書冒頭の「スマホは到達できない」は疑わしい。動画の状況はSSECから取れたときにしか起きない）。
- 確認：`tests/fetch-jma-typhoon.test.cjs`（5件）、`tests/typhoon.test.cjs` に6件（予報をたどる）、`tests/weather-playback.test.cjs` に1件（止まってボタンを待つ）。`tools/check-webgl.cjs` は、配信している予報が観測の時刻を覆うなら台風がそれに沿うこと（12時間以上先の最初の予報時刻で予報の中心から5km以内。それより前は合流中。最初は実況の時刻で測って、実データで43km離れて落ちた）、終わりで止まりボタンを出し自然には進まないこと、押すと溶けて戻ることを見る。予報が無いときは「覆う予報なし」として通す。

## シミュレーションも1時間ごとに見せる（2026-09-26・JS版のみ）
ユーザーの指摘：観測（1時間に1枚、雲が湧いて消えるので細かくちらつく）からシミュレーション（毎フレーム連続に動く）へ移ると急にぬるっと滑らかになる。「どちらかに合わせて」→ ア：計算を観測に合わせる、を選んだ（イ：観測の間を推定の動きで補う、は採らなかった）。
- 計算は今までどおり連続（0.5時間刻み）で進めるが、画面に出すのは**整数の時刻に達したときの状態だけ**。表示用の目標を2枚（`StormSimulation.shown`、今の時刻と1つ前）持ち、前の1時間から今の1時間へ観測の切り替わりと同じ長さ（`min(380ms, 再生の1コマ×0.55)`、なめらかにのスイッチがオフなら即座）で溶かす。前の時刻はテクスチャユニット6（計算の途中で背景の流れに借りるので、`restore()` のたびに `bindShown()` で戻す）、今の時刻は7。台風の輪も表示中の時刻（`shownHours`）の位置で、1時間ごとに進む。薄紫の色は最初の1時間の溶け込みとともに入る（観測から計算へ一瞬で色が変わっていた）。
- 検査：`tools/check-webgl.cjs` の「1時間ずつ進み、観測と同じく溶け込む」。一番遅い速さ（1秒で1.5時間）で4秒見て、同じ表示時刻の間に画面上の雲が一度も変わらないことと、溶け込みの途中を捕まえることを確かめる。**毎フレーム表示する元の動きに戻すと落ちる**（同じ時刻の間に5回変わった）ことを確認済み。

## 台風は気象庁の予報だけで動かす・雲の揺らぎ（2026-09-26・JS版のみ）
ユーザー：「模型にしたくありません」「気象庁のデータは台風の進路を適切に表現したいだけ」「（揺らぎは）入れて」。
- **予報は観測に合わせて選ぶ**：`scripts/fetch-jma-typhoon.cjs` は一番新しい報ではなく、同梱観測の最後の時刻（`--for`、既定は `dist/weather/sequence/manifest.json` の最後）に一番近い実況の報を擾乱ごとに選び（実況がその24時間後以内・予報がその12時間後以降まで、`forObservations`）、ファイルに `for` として書く。最初の版は3時間ごとに一番新しい報を取っていたので、観測から1日たつと新しい報が観測の時刻を覆わなくなり、台風が過去の平均の動き（段階①）に戻るところだった。
- **取るのは観測が変わったときだけ**：公開ワークフローは、コミット済みの `jma-typhoon.json` の `for` が同梱観測の最後の時刻と違うときだけ取る。`commit` 付きの観測取得では観測と一緒にcommitされ、プッシュで取った（同梱観測用の）ときは「Keep the typhoon forecast with the bundled observations」の手順が単独でcommitする（GITHUB_TOKENのpushは次の実行を起こさない）。**scheduleは外した**。気象庁のフィードは数日分しか持たないので、取り直しが遅れると合う報が消えていて台風は動かない（そのときは画面にそう出る）。
- **台風を動かすのは気象庁の予報だけ**：`simStorms`（`dist/app.js`）は予報が観測の時刻を覆う擾乱だけを返す。観測で見つけた台風（`storms.json`）は、予報の中心から600km以内なら合流の出発点として使うだけ。予報の無い台風（東太平洋など）は台風として動かさず、雲は他の雲と一緒に流れる。`followForecast` に `model` を渡さない。`typhoon.json`（470KB）はもうページが読まない（`dist/typhoon.js` と採点のスクリプトは残る）。
- **雲の揺らぎ**：運ぶだけの雲は形がほとんど変わらず、観測より滑らかに見えた（1時間の変化 観測0.064 対 計算0.024〜0.038、細かさ 0.038 対 0.024〜0.027、日本付近・雲の濃さ0〜1）。表示する各時刻の雲に、空気がどこから来たか（ずれの地図）の上に描いた3次元の値ノイズ（2オクターブ、時間方向にも滑らか）で、縁を膨らませたり削ったりし（`4c(1-c)` に比例）、晴れた所にもわずかに（`birth`）足す。強さ・格子・時間の相関は測って選んだ：`FLICKER = {amp 0.55, cell 1.8°, tau 1.5時間, birth 0.05}` で1時間の変化0.055〜0.062・細かさ0.030〜0.043（1時間ごとに独立な揺らぎ tau=1 でも数字は合うが、瞬きに見えるので少し時間をつないだ）。**見た目を観測に似せるためのもので、天気の計算ではない**と「この表示について」に書いた。
- **予報の後は典型的な消え方**（2026-09-26、ユーザー「北東に出ながら消えていくのが自然」）：予報の最後の時刻でまだ台風（`台風` かつ34kt以上）なら、そこで止めて薄れさせずに36時間延ばす（`followForecast(..., {decay:true})`、`DECAY_HOURS`・`DECAY_KMH`）。予報の最後の1時間の動きから始め、毎時 1−exp(−1/12) ずつ北東（南半球は南東）・時速35kmへ寄せ、強さは予報の最後の値から0へ直線で下げる。熱帯でまだ西へ進んでいる（東向き成分なし・緯度25°未満）ならその向きのまま。各点に `decay`（0〜1）。`storm-sim.js` はこれを `motion.w` として、渦巻きの帯を消し、厚い雲を進む向きへ最大250km先にずらして進む向きに3.5倍・横に1.3倍引き伸ばす。強さは予報の最後の強さ×(1−decay)（34ktを切った時点で消えないように）。同梱の予報（26号、最後 29.7N 134E 70kt）では36時間で北へ6.4°・東へ7.1°進み、雲は関東の沖を北東へ流れる。時速35km・36時間は典型値で、この台風の予報ではないと画面の注記と「この表示について」に書いた。`tests/typhoon.test.cjs` と `tools/check-webgl.cjs`（「予報の後、北東へ進んで消える」「予報の後だと画面が言う」、`--shots` で `13-simulation-past-the-forecast.png`）で固定。
- 検査：`tools/check-webgl.cjs` の「1時間の変化と細かさが観測と同程度」（同じ格子・同じ測り方で、同梱の最後の13時刻と比べて±40%以内）。揺らぎを0にすると落ちる（変化0.036 対 0.065）ことを確認済み。台風の印の検査は「予報のある台風だけが印を持つ」に変えた。`tests/fetch-jma-typhoon.test.cjs` は「観測に一番近く、覆う報を選ぶ・1日後の新しい報は選ばない」を固定。

## 台風の雲は観測を運ぶ・もやと筋を消す（2026-09-26・JS版のみ）
ユーザー：「観測データとシミュレーションが違いすぎて気持ち悪い」。同じ位置で最後の観測と+1〜96時間を並べると、違いは3つだった：①台風の雲を強さに応じて**作っていた**ので、24時間で観測の約3倍の滑らかな白い円盤になり、900kmまで届く回転が周りを渦巻きの輪に巻き込んだ、②緯度ごとの平均雲量へ近づける処理（240時間）が晴れた海に薄紫のもやをかけた、③雲が生まれも消えもせずシアーで伸びて、くしでとかしたような筋と点線になった。ユーザーの選択は「A（本物の台風の雲を運ぶ）とB（もやと筋を減らす）」。
- **A**：`dist/storm-sim.js` の RESOLVE で、各台風について**最後の観測の台風のまわりの雲**（`points[0]` の周り、半径350kmまで全部・650kmで0へなじませる、`PATCH`）を切り取り、今の中心に置く。強さに応じて回す（100ktで毎時6°、`SPIN_DEG_PER_HOUR`、`track.spin` に累積）。予報の後（`decay`）は進む向きに引き伸ばし、重みを (1−decay) で下げて**下の雲へ溶かす**。雲を作る処理（STORM・cloud・渦の風・台風の移動に周りを引きずる混ぜ合わせ）は削除し、背景の流れ（FLOW）は背景の風だけで運ぶ。**切り取った雲は元の場所から取り除く**（空気が最後の観測で台風の中心から350〜650km以内にあった所の雲を同じ重みで薄め、運んだ雲は足し算で置く。開始時刻では差し引きゼロで何も変わらない）。最初の版はこれをせず、ユーザーのスマホで、台風が北東で消えた後も出発点の雲が西へ流れて台湾の東に残っていた。検査「台風の雲が出発点に残らない」は、空気がその350km以内から来た全ての点の雲量を流れの地図から数える（元の場所だけを測ると雲が西へ流れ去るので古いコードでも通ってしまった）。実測 0.43→0.004、取り除かないコードでは 0.43→0.204 で落ちる（閾値は最初の1/4）。
- **B**：平均へ近づける処理（BASELINE・relax）を削除。かわりにずれの地図のヤコビアンの特異値の比（小さな円がどれだけ細長く引き伸ばされたか）が6〜14倍で雲を0へ薄める（`STREAK`）。点線と細い筋は消えるが、**雲は時間とともに減る**（60°S–60°Nの平均雲量 0.164 → 172時間後 0.091）。比の閾値4–10・6–14・8–20・なしで比べて6–14を選んだ（`this.streak` をページから変えて測れる）。東西の筋の度合い（30–60°の南北/東西の変化の比、観測1.0）はフェードしても変わらず96時間で約1.3で、筋そのものは流れのシアーから来ている。
- 計算中の色は紫を0.3から0.12へ弱めた（`dist/app.js` の `simTint`）。
- 検査：`tools/check-webgl.cjs` の「台風の雲は観測の雲を運んだもの」（24時間後の中心600km以内の平均雲量が最初の0.6〜1.5倍、900km以内の明るい画素が2倍＋50以内。実測 0.180→0.206・118→139）と「晴れた空にもやが増えない」（0.08〜0.35の画素の割合が最初の1.15倍＋0.005以内。実測 0.158→0.097）。**元のコードではどちらも落ちる**（0.242→0.480・111→370、0.158→0.378）ことを確認済み。`--desktop` の導入の検査はこの変更の前から落ちている（開始56色、未調査）。

## 地域の高解像度（2026-09-20・JS版のみ）
ユーザーの希望「関東だけのように地域までズームしてもよく見えるように」。全球512×512は日本付近で約64 km/画素なので、拡大すると雲が四角くなる。**同じ観測・同じ時刻を、SSECが持っている解像度のまま切り出した画像を別レイヤーとして重ねる**方式にした。拡大・補間・生成は一切しない。
- 取得：`node scripts/fetch-observations.cjs --span 72 --frames 72 --every 60 --winds --region`。既定は日本（南24°・西122°・北46°・東148°）、幅1280px（約2 km/画素＝赤外線band13の限界）、**最新12時刻だけ**（1枚が全球の数倍あるため）。`--region-bounds S,W,N,E`・`--region-width`・`--region-frames`・`--region-name` で変えられる。`dist/weather/region/manifest.json` に bounds・width・height・km/画素・各ファイルのURLとSHA-256を記録。**SSECに到達できる機械でのみ実行**。
- `--region` を付けずに取得すると切り出しは消え、空の manifest（`frames: []`）が書かれる。切り出しは切り出し元の観測に属するので、観測を入れ替えたら作り直す。空でも**ファイルは必ず置く** — 無いとページが404を出し、コンソールエラーと「配信されていないファイル」の検査に引っかかる。
- 表示：スイッチ「日本付近を細かく表示（高解像度の観測）」。**スイッチを入れたときだけ**読み込む（約N MBと画面に出す）。切り出しの無い時刻はその時刻の全球観測のまま（古い切り出しを残さない）。溶け込みも切り出し同士で行うので、画面上の2枚は常に同じ解像度。境目は縁2%でなじませる。切り出しにもSSECのロゴが入るので、全球と同じく画像のまま描く（白い雲に変換しない）。単体HTML版には含まない（容量）。
- 位置合わせの正しさは `dist/region-box.js` の1か所に集約（`mercator01` はシェーダーの `my` と `dist/weather.js` の `mercatorY` と一致することをテストで固定）。
- 検証：SSECに繋がらないので `node tests/make-region-fixture.cjs` が**同梱の全球観測を切って拡大した代用**を /tmp のdist複製に作る（manifestに `fixture: true`。観測ではない）。`node tools/check-webgl.cjs --dir <その dist>` が、切り出しONで絵が変わること（15.1%の画素）と、**粗い格子16×16の平均輝度がほとんど動かないこと**（0.5%）を測る。`--shift 2`（約180 km ずらした代用）で5.1%、`--shift 6` で13.3%になるので、閾値2%は位置ずれを捕まえる。**この代用に細かさは無い** — 実際に細かいかはSSECから取ってみるまで分からない。
- 2026-09-22、**実データが入った**：12時刻（09-21 14:00 〜 09-22 01:00 UTC）、1024×1071画素、3.1MB、**2.3 km/画素**（全球の約64 km/画素に対して28倍細かい）。実Chromeで確認：切り出しONで画面の1.3%の画素が変わり、粗い格子の平均輝度のずれは0.1%（位置は合っている）。
- **最初に取った1280幅は全面に「Size limit exceeded」とSSECのURLがタイル状に焼き込まれていた**。SSECは1回の画像要求が**面積で約117万画素**を超えると、画像に通知を書き込んで返し、`RE-Watermark: size N`（Nは線形の超過倍率）でそう申告する。実測：1024幅（約110万画素）clean、1100幅で1.04、1152で1.09、1200で1.14、1280で1.21。**通知入りの画像は観測でない**ので、`download()` がこのヘッダーを見て実行を中止する（`RE-Time` の照合と同じ扱い、`tests/fetch-observations.test.cjs` で固定）。切り出しだけ取り直すには `--region-only`。赤外線帯band13の実解像度が約2 kmなので、この上限は見える限度に近い。
- Unity版は未対応。C#とシェーダーの改修＋ユーザーのビルドが要るので、JS版で実物を見てからにする。

## 取得の条件と負荷（2026-09-22、ユーザーの質問で実測）
SSEC RealEarth の Terms of Use（原文を取得して確認）は「Public use … through its websites, devices, and **API is freely available**」で、条件は3つ：
1. **謝辞が必須** — 「Source: SSEC RealEarth, UW-Madison」。両ページに表示し、画像のロゴも消さずに描いている。
2. **可用性の保証なし** — 24/7でなく、人員は米中部時間の平日7:30–23:00。無人取得は失敗を前提にする（`RE-Time` 不一致で中止、ワークフローは `dist/` を巻き戻して同械の観測を公開）。
3. **無保証・免責**。

**1回の完全取得の実測（`--span 72 --frames 72 --every 60 --winds --region`）**：約232リクエスト、**約1.66 GB**。
- 観測画像は軽い：全球71枚13.97MB＋切り出し12枚4.45MB＋スナップ2枚1.16MB＝**19.6MB**（85リクエスト・約2分）
- **風が98.8%** — `/api/shapes` の1应答が AMV-LLlow **19.85MB** / AMV-LLmid **3.26MB**で、それを71時刻×2製品＝142回。**約1.64GB**を落として1°格子の71枚・3.19MBにし、残りは捨てている（約93秒、同时3本で約14MB/s）
したがって `--winds` なしなら約20MBで済む。**風を毎日全時刻取り直すのは大学のサーバーに対して重すぎる**。cron/scheduleはどのワークフローにも置いていない（トリガーは `push` と手動起動だけ。2026-09-26に気象庁の予報用の3時間ごとのscheduleを一度入れたが、観測と合わない予報に置き換わる害があって外した）。
削る手は3つ：①既に格子化してある時刻を再取得しない（実装済。今日の取得で 26 時刻約600MBが重複だった）、②**`--wind-every N`（実装済）**で風を間引く、③`/api/shapes` の `databounds`（未実装）。
2026-09-25、この軽い取り方で実際に取得した（ワークフロー手動起動、`wind_every 360`・`commit` オン、取得ステップ76秒）：全球71枚13.9MB・切り出し12枚3.8MB・スナップ2枚・風13時刻0.54MBで、**SSECへは約320MB**。同梱の動きの場で数えると、観測間の動きを決めるのは2°セルあたり **画像から測った動き約50% / 観測風約10% / 典型的な大気循環約39%** なので、風の間引きが効くのは約10%の領域だけ。風を13/71に落とした状態で110件超のテストと `--unity` 24項目が通ることも事前に確認した。
2026-09-25、公開ワークフローにも `wind_every` 入力を足した（**既定360＝6時間ごと**、空にすれば毎時）。スマホから手動起動するときの既定が1.6GBでは重すぎるため。①の再利用は前回と時刻が重なるときだけ効くので、数日空けた取得では効かない。間引いたときに「聞かなかった時刻」は風のmanifestに残らず、SSECが出していなかった時刻（`missing`）と区別がつかない（記録の追加は未実装）。
**日次更新の実費用**：新規は毎日 24 時刻なので、風を毎時取ると 24×23.1MB ＝ **約554MB**＋画像約9MB ＝ **約560MB/日**。`--wind-every 180`（3時間ごと）で **約194MB/日**、`--wind-every 360` で **約101MB/日**。風は観測間の模型の**第二の源**（第一は画像から測った動き）なので、間引いても見え方はほとんど変わらない。間引きは「**聞く相手を減らす**」だけで、既に持っている時刻の格子は消さない（`tests/fetch-observations.test.cjs` で固定）。
公開リポジトリに約24MBのSSEC由来画像を**再配布**している点は厳密には未確定。Termsは再配布を禁じておらず謝辞とロゴを保っているので通常は問題ないが、確定させるなら realearth@ssec.wisc.edu へ用途・URL・枚数を書いて一本メールを出す。

## 報告の形式（2026-09-25）
ユーザーへのチャットは**論文の形**で書く。①**要約**（200字程度・スマホ1画面・これだけで判断できること）②**目次**（3項目程度、多くて4）③**本文**（目次の順、節ごとに短く）。
付けるのは、説明が3段落以上になるとき・選択肢を比べるとき・作業の報告・設計の提案。**付けないのは**、1〜2段落で終わる答え、はい/いいえ、コマンド1つ、途中経過の一言、緊急の警告（結論を1行目に置く）。**要約と目次が本文より長くなるなら付けない** — 形式が中身を上回る。
書式：表は**3列まで**（4列は412px幅で横に切れる。自分で直したパネルと同じ失敗を文章で繰り返さない）。太字は1節に1つ。コードブロックはそのまま実行する物だけ。手順は目次でなく番号付きの並びにする。
中身の規則は形式とは別に常に守る：結論を先に／測った数字には単位と比較対象／事実・推測・未検証を書き分ける／失敗と未達を成果と同じ場所に／コミットIDとファイル名で後から確かめられるように。
この形式は**チャットだけ**。コミットメッセージ（履歴向け）・本書（次のセッションの私向け）・画面の文言（来訪者向け）は読み手が違うので、それぞれの書き方を続ける。

## 画面の約束
長い説明はパネル末尾の `<details class="notes">`「この表示について」に畳む。操作（ボタン・スイッチ・スライダー）の間に段落を挟まない。スマホで下のスイッチが遠くなり扱いづらいというユーザーの指摘による。
畳まずに常時出すのは、その操作を使う瞬間に必要な限界だけ：JS版は「白い雲は未校正の赤外線観測からの簡易合成」、Unity版はそれに加えて「観測と観測の間は推定した動きで、観測ではない」。文言そのものは消さず、畳んだ中に全文を残す。
スマホ（900px以下）では設定パネルは**1列**。1つの操作が1行を占め、その操作の注記はすぐ下に置く（`dist/style.css` 末尾）。以前は2列の格子で、注記が無関係なスイッチの横に並び、「台風の中心を重ねる（観測画像からの推定）」は3行に折り返して自分のスイッチに巻きつき、「初期の視点に戻す」は縦長の箱になっていた（2026-09-20、ユーザーのスクリーンショットによる指摘）。スイッチはラベルと切り替えが必ず同じ行（`.toggle{gap:14px}`・`.toggle span{flex:none}`）。`aside>input[type=range]` の `grid-column:2` は1列では暗黙の2列目を作ってしまうので打ち消してある。`tools/check-webgl.cjs` が412×915で「各操作が横幅いっぱいか」「スイッチのラベルが2行以内か」を測る（`--desktop` では測らない）。直す前のCSSに戻すと両方とも落ちることを確認済み。
`dist/unity/index.html` はビルド出力なので、テンプレート（`unity/MyAtras/Assets/WebGLTemplates/MyAtras/index.html`）と同じ変更を両方に当てる。次のUnityビルドでテンプレートから再生成されても同じ形になる。

## 開いたときの視点（2026-09-20）
ユーザーの希望で、**地球全体から東へ一周しながら日本全体のズームへ**降りる11秒の導入を付けた（`dist/app.js` の `INTRO`、`GlobeView.cs` の `Intro*`）。回転は全体の75%地点で終わり、寄りは55%地点から始まる — 寄った状態で回すと地表が流れるだけになるため。始点は日本の1周前（＝同じ経度）なので、太平洋→南北アメリカ→アフリカ→アジアと回って日本へ戻る。**最初の操作（ドラッグ・ピンチ・ホイール・キー・ズームボタン・戻す）でその場で終了**し、二度と動かない。最初の観測が出た時点で開始（4秒経てば観測が無くても開始）。`prefers-reduced-motion: reduce` のときはJS版は導入なしで最初から日本。Unity版は `SkipIntro` をC#に持たせ、**テンプレート側だけ**がそれを呼ぶ（今のビルドに無いメソッドを呼ぶとSendMessageの警告が出るため、`dist/unity/index.html` には次のビルドで入る。画面の約束の例外）。
着地点は変わらず、開いた瞬間に**日本全体が映る**位置。中央は東経136°・北緯35.5°（`yaw=2.374`・`pitch=0.620` ラジアン）、拡大は5倍。数値は両版で同じ（`dist/app.js` の `HOME`、`unity/MyAtras/Assets/Scripts/GlobeView.cs` の `HomeYaw/HomePitch/HomeZoom`）。「初期の視点に戻す」もここへ戻る。拡大の上限は1.7から8へ、拡大縮小ボタンは「0.12を足す」から「1.2倍する」に変えた（範囲が6倍になり、足し算ではボタンがほとんど効かないため。Unity側は `ZoomBy(delta)` を `zoom*(1+delta)` に読み替えるので、ページが送る±0.12はそのままでよい）。
視野は画面の**短い辺**で決まる（シェーダーは `p /= zoom*0.77` の正射影で、短辺の半分が球半径1に対応）。縦持ちのスマホ（412×622）で横±15°・縦±23°、PC幅（875×810）で縦±15°。与那国島（24.4°N 123°E）から稚内（45.6°N）まで入る。
**同梱観測は全球512×512なので、この拡大では観測1画素が画面の約10 px**（約63 km/画素）。台風の渦は形として見えるが、雲の縁は四角い。地表参考画像は2048×1024でやや甘い程度。本当に細かくするには日本付近だけの高解像度をSSECから取る必要があり、それはユーザーのPCからしかできない（コンテナからは403）。
2026-09-21、このPC（SHINYLABRY0）でUnity版をビルドして反映した（2分21秒・4MB・エラー0・警告0）。テンプレートから再生成された `dist/unity/index.html` に `SkipIntro` の行が入り、古いハッシュ名のBuildファイル4つは削除した（新旧が両方残ると約4MBの死荷重になる）。実Chrome（`--headless=new`）で導入の飛行を確認：全球（アフリカ→南北アメリカ→アジアと経度が変わる3コマ）→降下→日本。`prefers-reduced-motion` はこのヘッドレスでは `false` で、飛行が走ることも確認した。
`tools/check-webgl.cjs` は導入を2点で確かめる：読み込み直後に `window.geoIntro.running` が真であること、地球の脇の帯の色数が開始2色（＝ページの背景が見えている＝全球）から着地後512色（＝画面いっぱい）へ変わること。以降の検査は `window.geoIntro.skip()` で着地させてから測る（カメラが動いていると画素比較が汚れるため）。Unity版は導入中でも最初の操作（「−」20回）で終了するので検査はそのまま通る。
`tools/check-webgl.cjs` のUnity版「地球の周りに背景が見える」検査（Unityのアルファクリア退行を捕まえるもの）は、開いた視点が画面いっぱいになったため、先に「−」を20回押して引いてから測るようにした。

## 科学的な限界
白い層は未校正の赤外線輝度閾値0.38–0.82による簡易合成。冷たい地表を雲として含み、暖かい低層雲を見落とす。未来館と同じカラー合成を再現したものではない。地表参考画像はNASA Blue Marbleの雲のない地表で、雪氷は固定。球面に見える雲はすべて観測由来。観測範囲外や欠測を晴天と断定しない。

## 最初に行う作業
1. READMEと本書を読み、現在の表示を起動する。
2. 既存の3テストを実行し、Android幅で地球・操作・時刻の表示と再生を確認する。
3. ユーザーの次の希望を確認する。改善候補は低層雲や冷たい地表の誤判定を減らす合成と、観測間の滑らかな遷移。補間を導入するときは観測そのものと区別する。
4. 単なるテクスチャ平行移動や生成した雲を実観測として見せない。出典とロゴを維持する。

## 検証済みと未検証
`node tests/weather.test.cjs`（9件）、`node tests/weather-playback.test.cjs`（19件）、`node tests/cloud-model.test.cjs`（9件）、`node tests/cloud-simulation.test.cjs`（10件）、`node tests/observation-fade.test.cjs`（8件）、`node tests/fetch-observations.test.cjs`（14件・API模擬サーバー）、`node tests/build-motion.test.cjs`（6件）、`node tests/find-storms.test.cjs`（8件）、`node tests/storms.test.cjs`（15件・画面が何を描いてよいかの判定）、`node tests/build-tendency.test.cjs`（11件）、`node tests/region.test.cjs`（7件）、`node tests/typhoon.test.cjs`（19件）、`node tests/background-flow.test.cjs`（7件）、`node tests/fetch-jma-typhoon.test.cjs`（5件）の計148件（weather-playback 20件を含む）はこの環境で成功（2026-09-26）。同梱の動きと同梱の台風が、いずれも同梱の観測から作られたことも含む。同梱3,333ベクトルすべてが3時間後まで有限であることも確認済み。
Windows上のChrome（`--headless=new` + SwiftShader）で実描画を確認：配信版の合成表示・13時刻の再生、白黒観測モード、立体の模型モード（3,333地点）、および単体HTMLのfile://単体起動。地球・雲模型のGLSLは実ブラウザでコンパイル・リンク成功。
2026-09-17、Linuxコンテナの同梱Chromium（SwiftShader）とAndroid幅412×915のタッチ操作エミュレーションでも再確認：地球描画、13時刻が一巡してループ、一時停止で時刻が止まること、白黒観測トグル、可視光への切替（追加取得は失敗し、設計どおり表示中の観測を保持）、立体の模型（3,333地点の移流）、昼夜モード。約56fps。
Android実機でのUI QA、実機のタッチ操作、実ネットワークでの「最新を取得」は未検証。エミュレーションのタッチは実機の代わりにならない。
解決済：立体の模型の「↺ 戻す」。実装を読み実機で確認したところ、`elapsed` は正しく0になっていた。再生中は次のフレームから計算が進むため、毎秒5〜10分の速さでは目で追う前に数分に戻って見えていた。「戻す」は計算開始位置で停止するようにし（`dist/cloud-simulation.js`）、`tests/cloud-simulation.test.cjs` 10件で固定した。

2026-09-17、溶け込みを実ブラウザで確認：進捗0.4の瞬間の画面は前の観測とも次の観測とも異なり（画素差7.8%と8.6%、両端同士は9.7%）、溶け込みが実際に描画されている。トグルOFFで進捗は1に固定。

2026-09-20、Windows上のChrome（`--headless=new`）とAndroid幅412×915で両ページを再確認：`node tools/check-webgl.cjs` の10項目と `--unity` の14項目がすべて成功（項目はその後増え、2026-09-20時点でJS版20項目・Unity版21項目）。JS版は71時刻の再生・溶け込み（70サンプル中36で途中経過を捕捉）・タッチドラッグ、Unity版は「風で流す模型」での再生・太陽直下点の表示・一時停止。どちらもコンソールエラーなし。

2026-09-21、台風の印と見込みをJS版・単体HTML・Unity版で実ブラウザ確認：Unity版は琥珀283/235画素、紫136/12画素、観測のある時刻では12画素（＝非表示）。
2026-09-21、台風の印をJS版・単体HTMLの両方で実ブラウザ確認：観測時刻を台風のある時刻へ送り、スイッチON/OFFで琥珀色の画素が253→433に増えることと、表示文が「推定中心 20.9°N 149.8°E · …予報でもありません」になることを確認（`tools/check-webgl.cjs` に項目追加）。
2026-09-20、日本へ接近する台風を見るため観測を入れ替えた（72枚、09-17 05:00 〜 09-20 04:00 UTC）。`build-motion` を作り直し、7本69件のテストと両ページのheadless確認（JS版10項目・Unity版14項目）を通した。最新時刻の全球512×512では日本周辺が64×68画素で、台風は渦として判別できる。

出典・第三者データは `THIRD_PARTY_NOTICES.md` を参照。
取得の指針（誰がいつどこから取るか・各規約・負荷・観測と計算の区別）は `DATA_POLICY.md`（2026-09-26）。ブラウザからSSECへ取りに行く「1時間ごと」（既定オン、来訪者1人あたり毎時約6MB）と立体の模型の「観測風を更新」（1回約23MB）は、2026-09-26に**取り除いた**。ページが自分からSSECへ行くのは「最新を取得」を押したときだけで、画像の切り替えも同梱のスナップショットがあれば取りに行かない。`tools/check-webgl.cjs` の「nothing is fetched from SSEC unasked」（CDPのNetworkで数える）と `tests/weather.test.cjs` がこれを固定する。気象業務法の予報業務に当たるかは未確認。ユーザーは広く公開するつもりはないので問い合わせは急がない。
